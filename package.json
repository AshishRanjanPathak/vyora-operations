{
  "name": "vyora-operations",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install"
  }
}